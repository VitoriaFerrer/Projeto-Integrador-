document.getElementById("name="" method="" action=""").addEventListener("submit", function (event) {
    event.preventDefault();

    // Simulação de envio de formulário
    alert("Mensagem enviada com sucesso!");
    document.getElementById("name="" method="" action=""").reset();
});
